server.port=8080
# hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.driver-class-name=org.postgresql.Driver
# datasource
multitenant.defaultTenant=public
spring.datasource.username=${vcap.services.postgres-db.credentials.username}
spring.datasource.password=${vcap.services.postgres-db.credentials.password}
spring.datasource.url=jdbc:postgresql://${vcap.services.postgres-db.credentials.hostname}:${vcap.services.postgres-db.credentials.port}/${vcap.services.postgres-db.credentials.dbname}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.multiTenancy=SCHEMA
# jackson
spring.jackson.default-property-inclusion=non_null
# actuator
management.endpoints.web.exposure.include=beans,health,httptrace